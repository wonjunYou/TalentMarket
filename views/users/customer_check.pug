extends ../layout
block content

    .container-fluid
    .row
        .col-md-12
            #grid
                h1(style='font-size:70px;display:flex;justify-content:center;') My page
    .row
        br
    .row
        .col-md-1
        .col-md-2
          ul.list-group(style='margin:25px;font-size:auto;')
            li.list-group-item
              a(href=`/users/${currentUser._id}`)
                button.btn(style='margin:5px; width:100%;height:auto;' ) 회원 정보
            li.list-group-item
              a(href='/seller/join')
                button.btn(style='margin:5px; width:100%;height:auto;') 판매자 여부 선택
            li.list-group-item
              a(href=`/users/products/${currentUser._id}`)
                button.btn(style='margin:5px; width:100%;height:auto;') 구매 상품 확인
            li.list-group-item
              button.btn(style='margin:5px; width:100%;height:auto;' href='#') 후기 작성
            li.list-group-item
              a.need-confirm-btn(href=`/users/${currentUser._id}?_method=DELETE`)
                button.btn(style='margin:5px; width:100%;height:auto;') 회원탈퇴
        .col-md-7
            if !orders.total
                .alert.alert-info 구매 내역이 없습니다.
            else
                .orders
                    each order in orders.docs
                        .orders.row
                            img.col-md-3.fluid(src=order.product.img)
                            dl.col-md-9
                                dt.col-md-3 주문번호
                                dd.col-md-9= order.id 
                                dt.col-sm-3 상품명
                                dd.col-sm-9= order.product.title
                                dt.col-sm-3 가격
                                dd.col-sm-9= order.price
                                dt.col-sm-3 마감일
                                dd.col-sm-9= order.closingDate
                                a.btn.btn-sm.btn-outline-secondary.btn-primary.text-white(href=`/users/status/${order._id}`) 상황 보러가기
        .col-md-1  