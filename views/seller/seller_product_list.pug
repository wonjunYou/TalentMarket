extends ../layout

block content
    .container-fluid
    .row
        .col-md-12
            #grid
                h1(style='font-size:70px;display:flex;justify-content:center;') My page
    .row
        br
    .row
        .col-md-1
        .col-md-2
          ul.list-group(style='margin:25px;font-size:27px;')
            li.list-group-item
              a(href=`/seller/${currentUser._id}`)   
                button.btn(style='margin:5px; width:100%;height:auto;') 프로필 관리
            li.list-group-item
              a(href=`/seller/order/${currentUser._id}`)
                button.btn(style='margin:5px; width:100%;height:auto;' ) 진행 상황 입력
            li.list-group-item
              a(href=`/seller/products/${currentUser._id}`)
                button.btn(style='margin:5px; width:100%;height:auto;') 상품 관리
        .col-md-7
            ul.list-group(style='margin:25px;font-size:auto;')
                li.list-group-item
                  .container-fluid
                  .row  
                    .col-md-9 
                      h3 나의 상품 목록
                    .col-md-3
                        a(href='/seller/product/add')
                            button.btn(type="button",style="float:right;margin:10px;width:70px;background:gray;") 추가  
                  .row
                    if !products.total
                      p 등록된 상품이 없습니다
                    each product in products.docs 
                      .col-md-2
                        include ../products/_product