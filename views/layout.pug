doctype html
html
  head
    title Mj Mong
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    link(href='https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css', rel='stylesheet')
    script(src="https://kit.fontawesome.com/0d92f15091.js" crossorigin="anonymous")
    script(src='https://code.jquery.com/jquery-3.4.1.min.js') 
    script(src='https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js')
    script(src='https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js')
    script(src='/javascript/application.js') 
    script(src='/javascript/web3.js') 
    script(src="https://cdn.jsdelivr.net/gh/ethereum/web3.js@1.0.0-beta.36/dist/web3.min.js")
    //- script(src="https://cdn.jsdelivr.net/gh/ethereum/web3.js@1.2.0/dist/web3.min.js")
    //- script(src='/dist/web3.min.js')
    style.
      .hiding{
        display: none;
      }
      #category_list::-webkit-scrollbar{
        display:none;
        width:max;
      }
      #head{
      display:inline;
      text-align:center;
      display: block;
      background-color:skyblue;
      }
      h1{
      display:inline;
      color:black;
      }
      h2{
      display:inline;
      color:white;
      font-size:65px;
      }
      .nav-link{
      color:white;
      font-size: 18px;
      font-style: normal;
      }
      button{
      float:center;
      width:500px;
      }
      #top{
      display:flex;
      justify-content: flex-end;
      align-items:flex-end;
      background-color:skyblue;
      color:white;
      font-style: normal;
      }
      a{
      color:white;
      font-style: normal;
      }
      #grid {
          display:grid;
          margin:1px;
          padding:15px;
          background-color:skyblue;
          height:120px;
        }
      body {
      font: 1rem 'Montserrat', sans-serif;
      margin-top: 40px;
      padding-top: 1em; }
      body > .main-content {
      min-height: 400px; }
      #top{
        display:flex;
        justify-content: flex-end;
        background-color:skyblue;
        color:white;
        font-style: auto;
        }
      h1{
      display:inline;
      color:black;
      font-size:60px;
      }
      h2{
      display: inline;
      color:white;
      font-size:45px;
      }
      #head{
        display:inline;
        text-align:center;
        display: block;
        background-color:skyblue;
        padding-top:50px;
        padding-bottom: 50px;
      }
      #profile{
      display: flex;
      justify-content: center;
      }
      .product_view{
      position:relative;
      padding: 0 0 0 0px; width:900px; box-sizing: border-box;
      }
      #img{
      position:absolute; left:0; top:0;
      border:1px solid #e8e8e8;
      width:300px;
      }
      .total{
      font-size:30px; color: #0a56a9;
      }
      button{
      margin-left:40px;
      }
        #check:visited{
        font-weight:bold;
        backgroud-color:skyblue;
        }
        #product_img {
        width: 300px;
        height: 300px;
        }
        #grid {
        display:grid
        background-color:skyblue;
        }
        table,th,td{
        border: 1px solid gray;
        border-collapse:collapse;
        width:300px;
        }
        th{
        background-color:gray;
        text-align:center;
        }
        td{
        text-align:center;
        }

  body
    block top-nav
      include ./includes/topnav
    block hero-unit
    if flashMessages
      each texts, type in flashMessages
        .container
          .alert.alert-dismissible.fade.show(class=`alert-${type}`)
            each msg in texts
              | #{msg} 
            button.close(type='button', data-dismiss='alert', aria-label='close')
              span(aria-hidden='true') &times;
    
    block content

